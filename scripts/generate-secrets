#!/usr/bin/env bash
set -euo pipefail

node -e "console.log('ADMIN_PASSWORD=' + crypto.randomBytes(24).toString('base64url')); console.log('INGEST_TOKEN=' + crypto.randomBytes(32).toString('base64url')); console.log('SESSION_SECRET=' + crypto.randomBytes(32).toString('base64url'))"

